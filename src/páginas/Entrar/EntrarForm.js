import React from "react";
import useForm from "../../hooks/useForm";
import { useHistory } from "react-router-dom";


export const EntrarForm = () => {
    return(
        <div>
            Formulário para entrar
        </div>
    )

}